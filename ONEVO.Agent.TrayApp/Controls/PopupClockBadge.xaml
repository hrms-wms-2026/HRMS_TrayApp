<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ONEVO.Agent.TrayApp.Controls.PopupClockBadge"
             HorizontalOptions="Center">
  <Grid WidthRequest="64" HeightRequest="56">
    <Label Text="✦" FontSize="9" TextColor="{StaticResource BrandTitle}" Opacity="0.9"
           HorizontalOptions="Start" VerticalOptions="Start" Margin="4,0,0,0" />
    <Label Text="✦" FontSize="7" TextColor="{StaticResource Primary}" Opacity="0.75"
           HorizontalOptions="End" VerticalOptions="Start" Margin="0,2,2,0" />
    <Label Text="✦" FontSize="6" TextColor="{StaticResource BrandTitle}" Opacity="0.65"
           HorizontalOptions="End" VerticalOptions="End" Margin="0,0,8,4" />
    <Border WidthRequest="48" HeightRequest="48"
            StrokeShape="RoundRectangle 24"
            StrokeThickness="1.2"
            Stroke="{StaticResource SoftOutline}"
            BackgroundColor="{StaticResource PopupClockFill}"
            HorizontalOptions="Center"
            VerticalOptions="Center">
      <Label Text="{StaticResource IconClock}"
             FontFamily="Segoe MDL2 Assets"
             FontSize="22"
             TextColor="{StaticResource Primary}"
             HorizontalOptions="Center"
             VerticalOptions="Center" />
    </Border>
  </Grid>
</ContentView>
