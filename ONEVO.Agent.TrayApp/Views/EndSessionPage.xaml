<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ONEVO.Agent.TrayApp.ViewModels"
             xmlns:controls="clr-namespace:ONEVO.Agent.TrayApp.Controls"
             x:Class="ONEVO.Agent.TrayApp.Views.EndSessionPage"
             x:DataType="vm:EndSessionViewModel"
             BackgroundColor="{StaticResource Background}">

  <Grid RowDefinitions="Auto,*,Auto" Padding="20,12">
    <controls:AppHeaderBar Grid.Row="0" />

    <Grid Grid.Row="1" RowDefinitions="*,Auto,Auto,Auto" RowSpacing="10">

        <Grid x:Name="PaneGrid" Grid.Row="0" ColumnDefinitions="0.9*,1.1*" ColumnSpacing="20">
          <Grid x:Name="LeftPane" Grid.Column="0" MinimumWidthRequest="0">
              <Image Source="workspace_active.png"
                     Aspect="AspectFit"
                     HorizontalOptions="Fill" VerticalOptions="Fill" />

              <Border x:Name="CompletedBadge" WidthRequest="34" HeightRequest="34" StrokeShape="RoundRectangle 17"
                      StrokeThickness="0" HorizontalOptions="End" VerticalOptions="Start"
                      Margin="0,10,10,0" BackgroundColor="{StaticResource StatusGreen}">
                <Label Text="{StaticResource IconCheck}" FontFamily="Segoe MDL2 Assets"
                       FontSize="15" TextColor="White"
                       HorizontalOptions="Center" VerticalOptions="Center" />
              </Border>
          </Grid>

          <VerticalStackLayout x:Name="RightPane" Grid.Column="1" Spacing="8" VerticalOptions="Center">
            <HorizontalStackLayout Spacing="4">
              <Label Text="Workday " Style="{StaticResource PageTitle}" FontSize="22" />
              <Label Text="Completed" Style="{StaticResource PageTitleAccent}" FontSize="22" />
              <Label Text=" 🎉" FontSize="20" VerticalOptions="Center" />
            </HorizontalStackLayout>
            <Label Text="{Binding Message}"
                   FontSize="13" TextColor="{StaticResource TextSecondary}" />

            <!-- Top: Status | Clock Out Time | Total Shift (matches design TRAY-SUM-08) -->
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
              <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="10">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Status" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding StatusText}" FontSize="14" FontAttributes="Bold"
                         TextColor="{StaticResource Primary}" />
                </VerticalStackLayout>
              </Border>
              <Border Grid.Column="1" Style="{StaticResource GlassCard}" Padding="10">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Clock Out Time" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding ClockOutDisplay}" FontSize="14" FontAttributes="Bold"
                         TextColor="{StaticResource TextPrimary}" />
                </VerticalStackLayout>
              </Border>
              <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="10">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Total Shift" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding TotalShiftDisplay}" FontSize="14" FontAttributes="Bold"
                         TextColor="{StaticResource TextPrimary}" />
                </VerticalStackLayout>
              </Border>
            </Grid>

            <!-- Detail times once each -->
            <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
              <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="8">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Clock In Time" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding ClockInDisplay}" FontSize="13" FontAttributes="Bold"
                         TextColor="{StaticResource StatusBlue}" />
                </VerticalStackLayout>
              </Border>
              <Border Grid.Column="1" Style="{StaticResource GlassCard}" Padding="8">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Clock Out Time" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding ClockOutDisplay}" FontSize="13" FontAttributes="Bold"
                         TextColor="{StaticResource StatusBlue}" />
                </VerticalStackLayout>
              </Border>
              <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="8">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Total Work Duration" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding WorkingTimeDisplay}" FontSize="13" FontAttributes="Bold"
                         TextColor="{StaticResource StatusGreen}" />
                </VerticalStackLayout>
              </Border>
              <Border Grid.Column="3" Style="{StaticResource GlassCard}" Padding="8">
                <VerticalStackLayout Spacing="2">
                  <Label Text="Total Break Time" FontSize="10" TextColor="{StaticResource TextSecondary}" />
                  <Label Text="{Binding BreakTimeDisplay}" FontSize="13" FontAttributes="Bold"
                         TextColor="{StaticResource StatusOrange}" />
                </VerticalStackLayout>
              </Border>
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
              <Button Grid.Column="0" Text="Download Summary"
                      ImageSource="icon_download_white.png"
                      Command="{Binding DownloadSummaryCommand}"
                      Style="{StaticResource OrangePillButton}" />
              <Button Grid.Column="1" Text="Close App"
                      ImageSource="icon_close_white.png"
                      Command="{Binding CloseAppCommand}"
                      Style="{StaticResource RedPillButton}" />
            </Grid>
          </VerticalStackLayout>
        </Grid>

        <!-- Productive | Idle | Break Sessions (matches design TRAY-SUM-08) -->
        <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="8">
          <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="10,8">
            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
              <Label Text="Productive Time" FontSize="10" TextColor="{StaticResource TextSecondary}"
                     HorizontalOptions="Center" />
              <Label Text="{Binding ProductiveTimeDisplay}" FontSize="15" FontAttributes="Bold"
                     TextColor="{StaticResource StatusBlue}" HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>
          <Border Grid.Column="1" Style="{StaticResource GlassCard}" Padding="10,8">
            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
              <Label Text="Idle Time" FontSize="10" TextColor="{StaticResource TextSecondary}"
                     HorizontalOptions="Center" />
              <Label Text="{Binding IdleTimeDisplay}" FontSize="15" FontAttributes="Bold"
                     TextColor="{StaticResource TextPrimary}" HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>
          <Border Grid.Column="2" Style="{StaticResource GlassCard}" Padding="10,8">
            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
              <Label Text="Break Sessions" FontSize="10" TextColor="{StaticResource TextSecondary}"
                     HorizontalOptions="Center" />
              <Label Text="{Binding BreakSessionsDisplay}" FontSize="15" FontAttributes="Bold"
                     TextColor="{StaticResource StatusOrange}" HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="12,8">
          <VerticalStackLayout Spacing="6">
            <Label Text="Top Applications Used Today" FontSize="12" FontAttributes="Bold"
                   TextColor="{StaticResource TextPrimary}" />
            <CollectionView ItemsSource="{Binding TopApps}"
                            ItemsLayout="HorizontalList"
                            HeightRequest="72">
              <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:TopAppItem">
                  <Border Style="{StaticResource GlassCard}" Margin="0,0,8,0" Padding="10,8" WidthRequest="132">
                    <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
                      <Image Source="{Binding IconSource}"
                             WidthRequest="24" HeightRequest="24"
                             Aspect="AspectFit"
                             HorizontalOptions="Center"
                             IsVisible="{Binding IconSource, Converter={StaticResource IsNotNullConverter}}" />
                      <Label Text="{Binding Name}" FontSize="11" FontAttributes="Bold"
                             TextColor="{StaticResource TextPrimary}"
                             HorizontalOptions="Center" HorizontalTextAlignment="Center"
                             LineBreakMode="TailTruncation" MaxLines="1" />
                      <Label Text="{Binding Duration}" FontSize="12" FontAttributes="Bold"
                             TextColor="{StaticResource Primary}"
                             HorizontalOptions="Center" />
                    </VerticalStackLayout>
                  </Border>
                </DataTemplate>
              </CollectionView.ItemTemplate>
            </CollectionView>
          </VerticalStackLayout>
        </Border>

        <Label Grid.Row="3" Text="{Binding ErrorMessage}" TextColor="{StaticResource StatusRed}"
               FontSize="12"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}" />
    </Grid>

    <Grid Grid.Row="2" ColumnDefinitions="Auto,*" Padding="0,8,0,0">
      <HorizontalStackLayout Grid.Column="0" Spacing="6" VerticalOptions="Center">
        <Image Source="icon_settings.svg" WidthRequest="12" HeightRequest="12"
               Opacity="0.55" VerticalOptions="Center" />
        <Label Text="Settings" Style="{StaticResource FooterLabel}" />
      </HorizontalStackLayout>
      <controls:FooterStatusBar Grid.Column="1"
                                ConnectionLabel="Connected to OneXso Server" />
    </Grid>
  </Grid>
</ContentPage>
